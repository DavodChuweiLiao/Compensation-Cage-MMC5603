# Compensation-Cage-MMC5603

try to find some characteristic between the b_field near cell and near the wall, lets consider a linear relationship between the derivative over time, as we only care about the drift

now we start logging the data in mG, we know there is dc offset but we only care about the change we can apply a center the data first. probably then use the linear regression
